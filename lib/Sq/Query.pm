package Sq::Query;
use 5.036;

# This will be a data-structure to build Queries that then can be
# transformed to pure perl. Or maybe other targets. Maybe very Lisp like
# no priority at the moment, but i gather ideas about it.

1;